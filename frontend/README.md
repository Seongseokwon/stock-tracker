# Frontend

StockPulse UI — 순수 HTML/CSS/Vanilla JavaScript.

- `index.html` — 마크업
- `style.css` — 스타일 (다크 글래스모피즘)
- `app.js` — 상태 관리, API 호출, 차트 렌더링

개발 시 `backend` 서버를 실행한 뒤 http://localhost:3000 에서 확인합니다.  
API는 same-origin `/api/*`, WebSocket은 `/ws` 를 사용합니다.
